# packet_sniffer
This project shows how packet_sniffing is done in a local area network.
First we created a website using flask in python for fake login page and a sniffing code which help us to get the inforamtion about devices which are connected to the same network. 
